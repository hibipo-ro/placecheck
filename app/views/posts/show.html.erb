<div class="container" id="post_show">
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-8">
                <% if @user.picture %>
                    <%= image_tag @user.picture.url, :size => "60x60" %>
                <% else %>
                    <%= image_tag current_user.image.url %>
                <% end %>
                <%= @user.name %>
            <div class="btn btn-link">
                <%= link_to '編集する', edit_post_path %>
                <%= link_to '削除する', post_path, method: :delete, data: { confirm: '削除しますか？' } %>
            </div>
            <div class="card">
                <h1><%= @post.title %></h1>
                <%= @post.content %>
                
                <h4>いいね: <%= @post.likes.count %>件</h4>
                <% if current_user.already_liked?(@post) %>
                    <%= button_to 'いいねを取り消す', post_like_path(@post), method: :delete %>
                <% else %>
                    <%= button_to 'いいね', post_likes_path(@post) %>
                <% end %>
                <h5>いいねしたユーザー</h5>
                <% @post.liked_users.each do |user| %>
                    <li><%= user.name %></li>
                <% end %>

            </div>
            <canvas id="myRadarChart"></canvas>
            <%= image_tag @post.picture.url if @post.picture? %>

            <%= form_for([@post, @post.comments.build]) do |f| %>
                <%= f.text_field :body %>
                <%= f.submit('コメントする') %>
            <% end %>

            <% if @post.comments.any? %>
            <ul>
                <% @post.comments.each do |comment| %>
                <li>

                    <div class="balloon6 mx-auto card">
                        <div class="faceicon">
                            <% if comment.user %>
                                <%= image_tag comment.user.picture.url, class: 'mr-3', :size => '40x40' %>
                            <% else %>
                                <%= image_tag comment.user.image.url if comment.user %>
                            <% end %>
                         <%= comment.user.name if comment.user  %>
                         <%= link_to '削除する', [@post, comment], method: :delete, data: { confirm: '削除しますか？' }  if current_user == comment.user %>
                        </div>
                        <div class="says">
                            <%= simple_format(comment.body) if comment.body %>
                        </div>
                    </div>
                   
                </li>
                <% end %>
            </ul>
            <% end %>

        </div>
    </div>
</div>
<script>draw_chart();</script>

